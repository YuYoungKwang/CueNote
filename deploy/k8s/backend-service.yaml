apiVersion: v1
kind: Service
metadata:
  name: cuenote-backend
  namespace: cuenote
  labels:
    app.kubernetes.io/name: cuenote-backend
    app.kubernetes.io/part-of: cuenote
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cuenote-backend
  ports:
    - name: http
      port: 80
      targetPort: http
